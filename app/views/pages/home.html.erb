<div class="d-flex justify-content-center align-items-center vh-100">
  <div class="text-center">
    <h1 class="mb-4">📝 Tu Organizador de Tareas</h1>
    <p class="lead">Organiza, prioriza y completa tus tareas de forma eficiente.</p>

    <%= link_to 'Empezar ahora', new_task_path, class: 'btn btn-primary btn-lg mt-3', data: {turbo_frame: :new_task} %>
     <%= turbo_frame_tag :new_task %> <%# Destino %>
    <div class="mt-5 text-start">
      <h5>¿Qué puedes hacer?</h5>
      <ul class="list-unstyled">
        <li>✔️ Crear nuevas tareas</li>
        <li>📅 Asignar fechas límite</li>
        <li>✅ Marcar tareas como completadas</li>
        <li>🗂️ Organizar por prioridad o categoría</li>
      </ul>
    </div>

    <%# @tasks.each do |task| %>
    <%#= render "task", task: task %>
    <%= turbo_frame_tag :tasks do %>
    <%= render @tasks %>
    <% end %>

  </div>
</div>
