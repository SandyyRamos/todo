<div class="add-task-button">
  <%= link_to '+', new_task_path, class: 'btn-add-task', data: { turbo_frame: :new_task } %>
</div>

<div class="d-flex justify-content-center align-items-center vh-100 position-relative">
  <div class="text-center">
    <p class="date"><%= Time.zone.now.strftime("%A, %B %d, %Y") %></p>
    <h1 class="mb-4">📝 TO-DO LIST</h1>
    <p class="lead">Organiza, prioriza y completa tus tareas de forma eficiente.</p>

    <%= turbo_frame_tag :new_task %>

    <%= turbo_frame_tag :tasks do %>
      <%= render @tasks %>
    <% end %>
  </div>
</div>
